name: CD

on:
  push:
    branches:
      - master
  release:
    types:
      - created
  workflow_dispatch:

jobs:
  build_release_linux:
    name: Build Release Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install Dependencies
        run: .github/scripts/install-ubuntu.sh
      - name: Build
        run: |
          mkdir build && cd build
          cmake \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DENABLE_OPENMP=ON \
            ..
          make -j$(nproc)
      - name: Package
        run: |
          cd build
          make install DESTDIR=appdir
          # Strip binaries and libraries to reduce size
          find appdir -type f \( -name 'solvespace' -o -name 'solvespace-cli' -o -name '*.so*' \) \
            -exec strip --strip-unneeded {} + 2>/dev/null || true
          tar -czf neosolve-linux-x64.tar.gz -C appdir .
      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: linux
          path: build/neosolve-linux-x64.tar.gz

  build_release_windows:
    name: Build Release Windows
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v6
      - name: Install Dependencies
        run: .github/scripts/install-windows.sh
        shell: bash
      - name: Build
        run: .github/scripts/build-windows.sh release openmp
        shell: bash
      - name: Package
        shell: bash
        run: |
          cd build/bin/RelWithDebInfo
          # Remove DLLs not used by solvespace (Win32 GUI build, no VTK, no debug)
          rm -f vtk*.dll VTK*.dll 2>/dev/null || true
          rm -f Qt5*.dll 2>/dev/null || true
          rm -f *_debug.dll 2>/dev/null || true
          echo "Packaging $(ls *.dll | wc -l) DLLs"
          7z a -tzip neosolve-windows-x64.zip solvespace.exe *.dll
      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: windows
          path: build/bin/RelWithDebInfo/neosolve-windows-x64.zip

  build_release_macos:
    name: Build Release macOS
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v6
    - name: Install Dependencies
      run: .github/scripts/install-macos.sh ci
    - name: Build
      run: .github/scripts/build-macos.sh release arm64
    - name: Bundle dylibs
      run: |
        APP=build-arm64/bin/SolveSpace.app
        mkdir -p "$APP/Contents/Frameworks"
        dylibbundler -od -b -ns \
          -x "$APP/Contents/MacOS/SolveSpace" \
          -d "$APP/Contents/Frameworks/" \
          -p "@executable_path/../Frameworks/"
        # dylibbundler rewrites install names, invalidating the linker's
        # ad-hoc signatures. Apple Silicon refuses to load unsigned Mach-Os,
        # so re-sign dylibs first (deepest first) then the main executables.
        find "$APP/Contents/Frameworks" -name '*.dylib' -print0 \
          | xargs -0 -n1 codesign --force --sign - --timestamp=none
        codesign --force --sign - --timestamp=none "$APP/Contents/MacOS/solvespace-cli" || true
        codesign --force --sign - --timestamp=none "$APP/Contents/MacOS/SolveSpace"
        codesign --force --sign - --timestamp=none "$APP"
    - name: Create DMG
      run: |
        hdiutil create -volname NeoSolve -srcfolder build-arm64/bin/SolveSpace.app -ov -format UDZO NeoSolve.dmg
    - name: Upload artifact
      uses: actions/upload-artifact@v6
      with:
        name: macos
        path: NeoSolve.dmg

  upload_release_assets:
    name: Upload Release Assets
    needs: [build_release_linux, build_release_windows, build_release_macos]
    if: "!cancelled() && github.event_name == 'release'"
    runs-on: ubuntu-latest
    steps:
    - name: Download All Workflow Artifacts
      uses: actions/download-artifact@v8
    - name: Get Release Upload URL
      id: get_upload_url
      env:
        event: ${{ toJson(github.event) }}
      run: |
        upload_url=$(echo "$event" | jq -r ".release.upload_url")
        echo "upload_url=$upload_url" >> $GITHUB_OUTPUT
        echo "Upload URL: $upload_url"
    - name: Upload neosolve-linux-x64.tar.gz
      uses: actions/upload-release-asset@v1
      continue-on-error: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.get_upload_url.outputs.upload_url }}
        asset_path: linux/neosolve-linux-x64.tar.gz
        asset_name: neosolve-linux-x64.tar.gz
        asset_content_type: application/gzip
    - name: Upload neosolve-windows-x64.zip
      uses: actions/upload-release-asset@v1
      continue-on-error: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.get_upload_url.outputs.upload_url }}
        asset_path: windows/neosolve-windows-x64.zip
        asset_name: neosolve-windows-x64.zip
        asset_content_type: application/zip
    - name: Upload SolveSpace.dmg
      uses: actions/upload-release-asset@v1
      continue-on-error: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.get_upload_url.outputs.upload_url }}
        asset_path: macos/NeoSolve.dmg
        asset_name: NeoSolve.dmg
        asset_content_type: binary/octet-stream
